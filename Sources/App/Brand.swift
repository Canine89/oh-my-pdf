import AppKit

enum Brand {
    static let name = "oh-my-pdf"
    static let tagline = "PDF 교정과 주석 입력"
    static let accent = NSColor(srgbRed: 0.85, green: 0.10, blue: 0.12, alpha: 1)
    static let highlight = NSColor.systemYellow.withAlphaComponent(0.42)
    static let correction = NSColor.systemRed.withAlphaComponent(0.18)
}
